export type BrandConfig = {
  name: string;
  colors: {
    primary: string;
    secondary: string;
    accent: string;
  };
  assets: {
    logoLight: string;
    logoDark: string;
    favicon: string;
  };
};
